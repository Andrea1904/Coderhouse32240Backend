<div>
    <h1> Hola {{ user.name }} bienvenido! </h1>
    {{#if isAdmin}}
    {{#each food}}
        <div>
            <p style="color:red; font-size:20px"> {{this.name}}</p>
            <p style="color:blue; font-size:20px" >{{this.price}}</p>
        </div>

    {{/each}}
    {{else}}
        <p> No tienes permisos</p>
    {{/if}}
</div>